<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:ColorPicker.View.Maui;assembly=ColorPicker.View.Maui"
             xmlns:components="clr-namespace:ColorPicker.View.Maui.Components;assembly=ColorPicker.View.Maui"
             x:Class="ColorPicker.Maiu.App1.MainPage">
    <controls:ColorPicker SelectedColor="{Binding SelectedColor, Mode=TwoWay}" Margin="4">
        <controls:ColorPicker.ControlTemplate>
            <ControlTemplate>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="26"/>
                    </Grid.RowDefinitions>

                    <!--Color for select-->
                    <components:ColorSelectionView x:Name="PART_ColorSelectionView" MarkRadius="20" />

                    <!--Hue for select-->
                    <components:HueSelectionView x:Name="PART_HueSelectionView" Grid.Row="1" Margin="0,3"/>

                    <!--Alpha for select-->
                    <components:AlphaSelectionView x:Name="PART_AlphaSelectionView" Grid.Row="2" Margin="0,3"/>

                    <!--Selected color-->
                    <components:SelectedColorView x:Name="PART_SelectedColorView" Margin="0,3" Grid.Row="3"/>

                    <!--HEX-->
                    <Grid Grid.Row="4" Margin="0,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="35" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Label Text="HEX" />
                        <Label x:Name="PART_HexLabel" Grid.Column="1" />

                    </Grid>
                    
                </Grid>
                <!--<Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="50"/>
                        <ColumnDefinition Width="50"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="50"/>
                        <RowDefinition Height="26"/>
                    </Grid.RowDefinitions>                    
                    
                    --><!--Color for select--><!--
                    <components:ColorSelectionView x:Name="PART_ColorSelectionView" />

                    --><!--Hue for select--><!--
                    <components:HueSelectionView x:Name="PART_HueSelectionView" Grid.Column="1" Margin="3,0,0,0"/>

                    --><!--Alpha for select--><!--
                    <components:AlphaSelectionView x:Name="PART_AlphaSelectionView" Margin="3,0,0,0" Grid.Column="2" />

                    --><!--Selected brush--><!--
                    <components:SelectedColorView x:Name="PART_SelectedColorView" Margin="0,3" Grid.Row="1"/>

                    --><!--HEX--><!--
                    <Grid Grid.Row="2" Margin="0,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="35" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Label Text="HEX" />
                        <Label x:Name="PART_HexLabel" Grid.Column="1" />

                    </Grid>
                    
                </Grid>-->
            </ControlTemplate>
        </controls:ColorPicker.ControlTemplate>
    </controls:ColorPicker>
</ContentPage>